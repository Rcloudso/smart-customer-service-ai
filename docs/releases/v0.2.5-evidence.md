# v0.2.5 Release Evidence

## Scenario

1. Ask a question whose best FAQ retrieval score is below `0.55`.
2. Open **Admin → Knowledge Review** and inspect the captured retrieval snapshot.
3. Edit the answer and convert the item to an FAQ.
4. Search for the same question and confirm the linked FAQ is the first result.
5. Retry conversion and confirm no second FAQ is created.

## Automated Evidence

- `EMBED_PROVIDER=other npm test`: passed the explicit-escalation/LLM-failure route check, knowledge-review service checks, regression checks, server TypeScript, and client TypeScript.
- `EMBED_PROVIDER=other npm run eval:faq`: 11 cases; Top1 accuracy 100%, Top3 recall 100%, no-match accuracy 100%; source distribution `hybrid: 9`, `vector: 2`.
- `PLAYWRIGHT_CHANNEL=chromium npm run test:e2e`: 13/13 API and Chromium browser tests passed, including 401/403 auth boundaries, validation, SSE compatibility, review deduplication, message-only/exact-message/legacy session ratings, reason/pagination/special-character filters, explicit escalation separation, conversion idempotency, post-conversion FAQ search, bilingual/theme behavior, and 390px responsive checks.
- `CAPTURE_RELEASE_EVIDENCE=1 PLAYWRIGHT_CHANNEL=chromium npx playwright test --project=web-chrome --grep "admin reviews a knowledge gap"`: passed and regenerated the screenshots below from the final UI.
- `EMBED_PROVIDER=other npm run build`: production server and Vite build passed. The existing non-blocking PostCSS module-type warning remains.
- `git diff --check`: passed.

## Screenshot Evidence

- `assets/v0.2.5-review-detail.png`: pending item with answer-time retrieval evidence.
- `assets/v0.2.5-converted.png`: converted state with index synchronization success feedback.
